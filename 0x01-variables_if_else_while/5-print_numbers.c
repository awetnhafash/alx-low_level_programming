#include <stdio.h>
/**
 * main - prints all single digit base ten numbers starting from zero
 * Return: always 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)

	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
