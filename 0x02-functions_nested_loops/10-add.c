#include "main.h"
/**
 * add - adds two integers and returns the sum.
 * @a: first integer.
 * @b: second integer.
 * Return: sum of the two given integers.
 */
int add(int a, int b)
{
	return (a + b);
}
