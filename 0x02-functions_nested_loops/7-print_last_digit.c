#include "main.h"
/**
 * print_last_digit - prints last digit of a number.
 * @i: an integerr input numner
 * Return: last digit
 */
int print_last digit(int i)
{
	int ld;
	
	ls = i % 10;
	if (ld < 0)
	{
		_putchar(-ld + 48);
		return (-ld);
	}
	else
	{
		_putchar(ld + 48);
		return (Ld);
	}
}
